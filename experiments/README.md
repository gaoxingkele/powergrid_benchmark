# Experiments

```text
experiments/
  smoke/    Minimal checks for configs, data availability, and baseline code.
  runs/     Timestamped or stable run outputs.
```

Experiment outputs should include enough metadata to reproduce the run: config path, git commit if available, environment, random seeds, and command line.
