# Method

MA-SQLGrid constructs compact schema/value context, normalizes domain values, infers answer shape, generates SQL with a fixed non-frontier generator, and optionally applies reference-free execution, shape, and value validation before final selection.
