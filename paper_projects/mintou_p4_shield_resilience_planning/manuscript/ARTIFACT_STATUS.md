# Artifact Status

- `MANUSCRIPT.md` is the canonical manuscript source for this stage.
- `../evidence/manifests/p4_s4_results_artifact_manifest_20260813.json` is the canonical results-artifact source map. It hash-pins the current planning, inference, mechanism-control, sensitivity, AC, and boundary sources; classifies identical-range replicate blocks separately from active configuration changes; and records the screening quality--call and AC-mapping interpretation limits.
- `figures/build_results_artifacts.ps1` verifies that manifest before rebuilding four stage-4 figures and four derived CSV tables. `../evidence/manifests/p4_s4_results_artifact_build_record.json` records their output hashes. No new optimizer or AC experiment is run by this generator.
- `EQUATION_IMPLEMENTATION_CONTRACT.md` records the equation--configuration--code mapping and the scopes that must be preserved when regenerating derived artifacts.
- `../experiments/p4_s3_boundary_predeclared.json` and `../experiments/p4_boundary_experiments.py` control the new p4-only boundary study. The frozen source profile, 1050 complete run rows, bounds, summaries, gaps, and immutable manifest are under `../evidence/`; `python experiments/p4_boundary_experiments.py verify` checks their hashes and within-setting bound sharing.
- No shared p3/p4 planning source was changed. The boundary NSGA-II+Repair execution uses the installed pymoo 0.4.1 API and is explicitly supplementary to the historical main archive.
- The controlling stage-4 PNG figures were regenerated in this worktree with System.Drawing because the WindowsApps Python launcher is blocked. The new AC figure uses the canonical six-network/108-case-per-method summary and replaces the stale 72-case panel in the manuscript. Legacy PDF/SVG variants with older filenames are unreferenced.
- The official preview builder refreshed `journal_submission/paper.tex` and copied the stage-4 figures, then stopped because this execution environment has no `pdflatex`. The existing `journal_submission/paper.pdf` predates stage 4 and is therefore not a current preview or an evidence source. A later acceptance run with a LaTeX engine must rebuild it.
- `submission_preview/` is a legacy pre-stage preview and is not a controlling manuscript or evidence source.
