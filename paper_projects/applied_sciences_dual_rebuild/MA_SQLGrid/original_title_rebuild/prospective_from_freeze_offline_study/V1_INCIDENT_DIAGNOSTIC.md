# Offline Coordination Study v1 — Retained Diagnostic Incident

The v1 directory is immutable and excluded from R2 claim promotion. An
independent audit found that the three files named as insertion permutations
had the same SHA-256, so they represented only one unique witness database.
Although the v1 selection and reproduction outputs are retained, the label
“complete three-state invariance” was too strong. V1 must not be pooled with or
substituted for v2.

R2 uses the separately frozen v2 protocol with three query-blind witness
operators and three distinct database hashes. No v1 file was resumed,
overwritten, deleted, or incorporated into v2 denominators.
