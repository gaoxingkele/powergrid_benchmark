# MA-SQLGrid target and alias

- Internal alias: **CMC / MA-SQLGrid**
- Actual intended journal: **MDPI Applied Sciences (Basel)**
- Suggested initial section routing: **Computing and Artificial Intelligence**
- CMC-era files: retained as the source baseline and provenance archive

The current PDF, Word and LaTeX files retain `CMC` in their filenames.  New MDPI
files should use an `AppliedSciences` suffix and must not overwrite the CMC-era
files.

The Applied Sciences version should emphasize the concrete maintenance-database
workflow, operational beneficiary, validation, robustness and reproducibility,
rather than presenting the paper as a generic LLM-agent architecture.
