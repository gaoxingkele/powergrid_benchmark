A manuscript teaser and architecture upgrade figure for a paper on C2GES: Causal and Counterfactual Graph-Enhanced Extractive Summarization for power-grid reliability reports. The figure should present a clear visual story: a role-conditioned question over NERC report sentences is converted into transparent sentence-level scores, and the system returns selected evidence sentence IDs rather than generating an unsupported free-form causal explanation.

Use a wide 16:9 academic graphical-abstract composition with a clean left-to-right flow. The left side shows the input boundary: public NERC report excerpts, sentence IDs, and a causal-role question such as "root cause?" or "mitigation?". The center shows the C2GES reranking mechanism as three parallel, interpretable scoring lanes that feed a compact fusion and ranking block. The right side shows the output boundary: top-K selected evidence sentence IDs and an optional downstream summary wrapper. The claim boundary is critical: the figure must show sentence-level evidence selection in report text, not expert adjudication, not power-flow simulation, not a learned full causal graph, and not a complete operational reconstruction of a grid event.

=== INPUT AND TASK ===
Create a very faint grey #F7F7F7 background panel labeled "INPUT + TASK" in small-caps Steel Blue #0072B2. Inside it, use white rounded-rectangle boxes with thin grey borders. Include a small stack of document pages labeled "NERC report sentences S = {s1...sn}" with tiny line snippets and visible sentence ID chips. Include a compact question card labeled "role-conditioned query q, r" with five small role chips: "trigger", "root cause", "response", "impact", "mitigation". Use a thin arrow labeled "candidate sentence pool" pointing to the method panel.

=== C2GES RERANKING CORE ===
Create a central very faint blue-grey #F2F6FA method panel labeled "C2GES RERANKING CORE". Inside it, show three parallel white module boxes with Steel Blue #0072B2 borders and dense internal mini-visualizations:
1. "Query relevance Q(s,q)" with a tiny monochrome text-matching heatmap and a small vector dot-product icon.
2. "Role compatibility R(s,r)" with five role chips connected to sentence cue tokens such as "fault", "relay", "load shed", "repair"; this lane should be visually emphasized because it is the dominant causal-role mechanism tested by the no-role ablation.
3. "Local chain consistency G(s,D,r)" with a small local window of neighboring sentence nodes connected by thin arrows, labeled "lightweight causal-chain signal"; keep it visibly auxiliary, not a full graph neural network.
Arrows from all three lanes feed a white "Fusion + ranking" box with formula-style short text: "Score = wq Q + wr R + wg G". Beneath it, show a small ordered list thumbnail: "s42 > s15 > s08".

=== OUTPUT AND EVIDENCE BOUNDARY ===
Create a right-side output panel with a faint green #EEF7EF background and thin Bluish Green #009E73 border. Label it "SELECTED EVIDENCE". Inside, show "top-K sentence IDs" as three chips: "ID_42", "ID_15", "ID_08". Add a small note-style icon labeled "extractive support". Show a very light dashed arrow from these IDs to a small downstream "summary packaging" note, making clear that the selected IDs are the evaluated output and any prose summary is downstream packaging.

=== GLOBAL ANNOTATIONS ===
Use small formula annotations near arrows: "D_i, q_i,r -> Y_i,r^K", "K = 3", and "auditable score decomposition". Add a tiny legend in the lower-right: Steel Blue = core scoring, Warm Orange = role cue emphasis, Bluish Green = selected evidence, Grey = auxiliary/document context. Keep all labels short and legible; avoid long paragraphs inside the bitmap.

STYLE SPECIFICATIONS:
Use a polished IEEE Access / SCI journal figure style with pure white module fill #FFFFFF, faint grey section backgrounds #F7F7F7, Steel Blue #0072B2 for core borders and main arrows, Warm Orange #E69F00 for the role-compatibility emphasis only, Bluish Green #009E73 for selected evidence, Light Grey #CCCCCC for secondary borders, Dark Grey #4D4D4D for arrows, and Charcoal #333333 for labels. Use flat colors, thin 1.2 px borders, no gradients, no drop shadows, no decorative blobs, no photo-realistic elements, and no colorful background blocks. Every module must contain internal content such as miniature text lines, chips, formulas, or tiny heatmaps. The claim boundary must remain visible: role-aware sentence-ID evidence reranking, not operational simulation, not expert-labeled ground truth, not a full causal graph learner, and not a generative explanation system.
