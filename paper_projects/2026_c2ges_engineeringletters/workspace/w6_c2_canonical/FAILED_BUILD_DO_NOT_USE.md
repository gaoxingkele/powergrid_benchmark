# Failed Preliminary Build — Do Not Use

This directory is a rejected preliminary W6 rendering. Its caption contract
failed because one caption omitted the explicit literal `NO-GO` marker.

The validated canonical release is `../w6_c2_canonical_v2/`, which contains a
passing `validation.json` and `canonical_manifest.json`.
