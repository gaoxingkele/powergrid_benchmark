# Decision Log: 2026-07-07

## Decisions

1. 将 `opsd_time_series` 从 metadata-only 提升为 downloaded，缓存 60-minute singleindex CSV。
2. 将 `psml` 从 planned 提升为 downloaded，缓存 GitHub 仓库。
3. 用具体可追踪仓库替代笼统的 `public_dga` 和 `pmu_fault_candidates`：
   - `dgann_duval`
   - `dgadb`
   - `lbnl_pmu_event_library`
   - `gridstage`
4. 将 ENTSO-E 和 PJM 从 planned 改为 metadata-only，保存本地入口页。
5. 对需要凭证或 TB 级数据的数据源，不强行伪装成 fully downloaded，保留 API-ready 状态。

## Consequences

- 当前 manifest 共有 19 条，其中 12 条 downloaded，7 条 metadata-only。
- 数据审计脚本可确认所有 manifest 路径均存在。
- 后续拿到 API key/token 后，可在现有目录下补充真实数据文件。
