# External Repositories

Clone upstream GitHub repositories here using `owner__repo` names.

```text
external_repos/
  owner__repo/
```

Record candidate repositories in `repo_candidates.csv` before cloning. For every cloned repository, write integration notes in the matching `paper_projects/<paper_id>/repo_analysis.md`.
