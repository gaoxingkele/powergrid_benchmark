# Mintou Portfolio Status

Status date: 2026-07-13

## Engineering Coverage

| Paper | Algorithm | Target | Main Exp. | Ablations | Baselines | Smoke Runs | Public Benchmark | Current Signal |
|---|---|---|---:|---:|---:|---:|---|---|
| `mintou_p1` | `DSTAR-GRU` | IEEE Access (framework) | 7 | 6 | 8 | 525 | Curtailment v5 onset eval: no defensible edge on any of 3 evaluation angles; retrieval hurts 24h onset | `route_a_framework_pivot_executed` |
| `mintou_p2` | `CSA-LoadNet` (ex `HyG-LoadFormer`) | Electronics | 8 | 7 | 9 | 680 | v7 (10 seeds + Ausgrid): OPSD 24h sig. beats MLP; hyperbolic components inseparable everywhere | `route_a_executed_csa_loadnet` |
| `mintou_p3` | `CARS-MODE` | Energies | 7 | 8 | 6 | 525 | Real-MOEA planning v6 (30 seeds, Wilcoxon+Holm) + AC validation v2 | `positive_but_partially_significant` |
| `mintou_p4` | `SHIELD-MOEA` | Energies | 8 | 7 | 5 | 520 | Real-MOEA planning v2 (30 seeds, Wilcoxon+Holm) + AC validation v2 | `significant_public_signal` |
| `mintou_p5` | `TRACE-MOEA` | IEEE Access | 7 | 8 | 6 | 525 | v1 review pipeline deprecated (circular); v2 real algorithms, 30 seeds, Wilcoxon+Holm | `positive_but_partially_significant` |
| `mintou_p6` | `BiLo-NSGA` | Applied Sciences | 8 | 9 | 6 | 640 | v1 review pipeline deprecated (circular); v2 real algorithms, 30 seeds, Wilcoxon+Holm | `significant_public_signal` |

## Boundary

Synthetic smoke outputs validate code paths, ARA evidence wiring, method naming, baseline coverage, ablation coverage, and result-analysis generation. They must not be used as final manuscript performance claims.

P1 now has an RTS-GMLC benchmark-derived dispatch experiment with fixed, rolling, and stress-subset evidence. After preserving v1 weak and v2 marginal evidence, DSTAR-GRU gains 0.08% over the strongest baseline on the fixed split, 0.07% on rolling windows, and 0.72% on the high-renewable stress subset. OPF/UC validation is still required.

P2 now has OPSD and SimBench public-data benchmarks plus rolling temporal split evidence. OPSD 24h/day-ahead forecasting has a strong fixed and rolling signal against stdlib baselines; SimBench 24h has fixed and rolling normalized-MAE value. The 1h task remains a recorded limitation.

P2 (2026-07-13): real neural baselines added (`mintou_neural_forecasting.py`: MLP/LSTM/TCN/DLinear/PatchTST-lite, identical splits and test sets, 3 seeds each, PyTorch CPU). Decisive honest result: MLP ranked first in all four settings against the ridge implementation (OPSD 24h -14.6%, 1h -55.6%, SimBench -21/-24%).

P2 (2026-07-13, upgrade): HyG-LoadFormer reimplemented as a genuine neural model (`mintou_hyg_neural.py`: Poincare-ball series embeddings, target-adaptive-curvature hyperbolic graph attention, shared temporal MLP encoder at MLP parameter budget; 5 real mechanism-switch ablations; identical protocol). Neural HyG beat every external baseline in 3/4 settings at 3 seeds.

P2 (2026-07-14, v7 verdict): 10 seeds for HyG variants + MLP, plus the Ausgrid solar-home hierarchical benchmark (12 customers + 4 regions + total, 17 series, 24h, sMAPE primary) and Mann-Whitney/Holm tests (`mintou_hyg_significance.py`). **Final component verdict: (1) the day-ahead main claim survives with significance — OPSD 24h HyG beats MLP (p=0.0085) and the no-graph ablation (p=0.0011); (2) the hyperbolic claim is unsupported — hyperbolic vs Euclidean vs equal-weight vs fixed-curvature are inseparable in all 5 dataset/horizon settings; (3) on the hierarchical Ausgrid set HyG significantly loses to DLinear (p=0.0044)**. Decision required: (A) downgrade the claim to cross-series aggregation, retitle, anchor on OPSD 24h, report inseparability honestly — submittable to Electronics/Applied Sciences relatively quickly; or (B) redesign so hyperbolic geometry actually touches the hierarchy (tree-initialized/regularized embeddings, cross-level reconciliation) — effectively half a new paper. Evidence chain v5→v6→v7 fully preserved. **Route A executed (2026-07-14, user decision): the method is renamed CSA-LoadNet (Cross-Series Attention Load Forecasting Network), the title drops Hyperbolic/Smart-Dispatch and is scoped to day-ahead multi-region forecasting, and the claim system (claims.md) is anchored on the significance-backed OPSD 24h wins with the weight-form inseparability reported as an honest negative finding; target journal remains Electronics.**

P3 (2026-07-13, second pass): the planning pipeline itself was rewritten to real algorithms (`mintou_real_planning.py` v6; proxy-method artifacts preserved as `*_proxy_methods_deprecated.*`). CARS-MODE is now a genuine binary MODE (jDE self-adaptive F/CR + SaDE strategy pool + budget repair + crowding diversity) against pymoo NSGA-II/MOEA/D and real scalarized DE/PSO/GA. Result: **+6.34% mean hypervolume over NSGA-II, 42/42 Holm-significant baseline wins**; but the FixedDE ablation edges the full method by 0.60% (2 significant losses) — strategy adaptation is a slight negative contributor on proxy HV. AC validation v2 (real-MOEA compromise plans, v1 preserved): CARS-MODE mid-pack (0.611 vs No-Plan 0.500; Standard DE 0.681 top) — its DER-heavier plans trade AC feasibility under the high-DER scenario; FixedDE drops to 0.569, so strategy adaptation helps at the AC level even though it costs proxy HV. Both trade-offs must be reported honestly. Note: the v1 voltage/hosting "constraints" were unsatisfiable within budget (v1 soft-penalized them silently); v2 makes budget the only hard constraint and reports planning-target shortfalls descriptively.

P4 (2026-07-13, second pass): same real-MOEA rewrite (SHIELD-MOEA = NSGA-II core + hybrid GA/DE variation + adaptive worst-K scenario screening + repair; search and final-evaluation scenario sets use disjoint seeds so screening cannot leak into scoring; worst-case HV reported as robustness readout). Result: **+5.56% over NSGA-II, 40/40 Holm-significant, zero significant losses — `significant_public_signal`**. Component analysis: repair is the main contributor (NoRepair 0.252 vs 0.274); scenario screening adds little on mean HV (+0.5%) but is cheaper; NoResilienceObj edges the full method by 0.26% (not significant). AC validation v2: SHIELD-MOEA ties top AC-feasible rate (0.708 vs No-Plan 0.500), and **NoOutage drops to 0.625 — the outage-aware search component now has real AC-level differentiation evidence**.

P5 (2026-07-13): the v1 project-review pipeline was found to be circular (hand-parameterized method proxies; metric consumed method-owned constants) and is preserved as `*_v1_deprecated_circular.*`. The v2 rewrite uses real algorithms (pymoo NSGA-II/MOEA/D, real AHP-TOPSIS/greedy/weighted-sum, self-contained TRACE-MOEA with preference coevolution + budget repair + trace archive), method-independent standard hypervolume, 30 seeds per method/experiment, and Mann-Whitney U + Holm. Result: TRACE-MOEA +0.89% over NSGA-II, Holm-significant in 38/42 baseline comparisons; but the best ablation (NoScheduleRisk) is only 0.13% behind and significantly wins one experiment, so the preference-coevolution component contribution is weak. Trace metrics are now descriptive-only. Expert labels and cost calibration remain required.

P6 (2026-07-13): same v1 deprecation and v2 rewrite (BiLo-NSGA implemented as NSGA-II core + real bidirectional local search + dependency moves + feasibility recovery). Result: +1.57% over NSGA-II, Holm-significant in 44/48 baseline comparisons, zero significant losses; however the NoBackwardSearch ablation edges the full method by 0.16%, i.e. the backward-deletion component is a slight negative contributor — report honestly or refine the operator. Expert labels and cost calibration remain required.

## Review Response (2026-07-14)

Third-party round-2 reviews (`reviews/2026-07-13_round2_*`) verified by six independent checks: all six reviews credible (spot-checked numbers accurate); roughly half of p2/p3/p4 findings already resolved by the 07-13/14 rewrites the reviewers could not see. Four review claims rejected/corrected (p5 "2/7 insignificant" is actually 4/7; p5-to-Access routing contradicts the review's own Access assessment — p5 stays Energies; p3 "drop strategy-adaptive" superseded by the HV-vs-AC trade-off evidence; p6 "7-10 days" corrected to 10-14 days writing). One review finding corrected OUR record: p1 ablations are fully run on RTS (label fix only). Consolidated roadmap: `reviews/2026-07-14_round2_response_and_roadmap.md` — submission order p6(W2-3, AppSci) → p3(W3-4, Energies) → p4(W4-5, Energies) → p2(W4-6, Electronics, pending A/B decision) → p5(W10-12, Energies, after external ground-truth backtest) → p1(W12+, curtailment-pivot evaluation first).

## Next Upgrade Path

1. Add PGLib/MATPOWER OPF or Grid2Op feasibility validation for P1.
2. P2: v7 evidence complete; Route A (claim downgrade to CSA-LoadNet) executed 2026-07-14 per user decision. Remaining: related work, sensitivity analysis, optional Panama dataset, figures, MDPI back-matter, manuscript drafting (see JOURNAL_REVIEW.md progress update 4).
3. P3: real-MOEA rewrite DONE (2026-07-13). Remaining: nodal siting/sizing AC experiments (composition-level screen leaves CARS-MODE mid-pack); reconcile the proxy-HV vs AC-feasibility trade-off in the manuscript narrative; consider refining strategy adaptation (slight negative on proxy HV, positive on AC screen).
4. P4: real-MOEA rewrite DONE (2026-07-13) with `significant_public_signal`. Remaining: monetary cost calibration; element-level N-1/N-k outage model if the resilience claim needs deepening.
5. Add expert-labeled feasibility review outcomes and cost calibration for P5 and P6; consider refining/dropping the backward-deletion operator in BiLo-NSGA (ablation shows slight negative contribution).
6. Preserve failed or weak runs in ARA traces instead of deleting inconvenient evidence (v1 circular pipeline preserved as deprecated artifacts).

## Decision Execution (2026-07-14)

Three user decisions executed:

1. **P2 Route A**: renamed to CSA-LoadNet, title/claims rescoped to day-ahead multi-region cross-series aggregation (see p2 paragraph above); target Electronics.
2. **P1 curtailment pivot (v4)**: the v3 dispatch pipeline had the proxy-method disease (hand constants + a DSTAR-exclusive renewable-bias formula that manufactured the ~30x curtailment gap), so the pivot required a full rewrite (`mintou_real_curtailment.py`: fixed 70%-SNSP reference policy, real GRU + learned-embedding Siamese retrieval, 6 baselines + 5 mechanism ablations, 4320h, 10 seeds, Mann-Whitney+Holm). **Honest verdict: the component story is now validated (1h: significant vs ALL learned baselines and ALL ablations, NoSiamese p=0.0004) but the method loses to naive persistence at both horizons (1h -6.4%, 24h -51.6%) — curtailment is too persistent/sparse for a superiority claim. Next decision: onset/transition-slice evaluation (cheap) or pivot to the retrieval-as-a-service framework framing.**
3. **P5/P6 NERC rule backtest (v1, ladder rung 1)**: `mintou_review_backtest.py` — rule = NERC-topic kind weights x NERC-independent raw stress percentile; TRACE-MOEA/BiLo-NSGA priority capture 1.34-1.62 (>1, consistency floor passed) but AHP-TOPSIS aligns most by construction (capture up to 2.3, tau ~0.5 significant) — citable only as external CONSISTENCY, not superiority. MISO MTEP historical backtest remains the required true anchor (ladder rung 2).

**P1 final experimental verdict (2026-07-14 v5)**: full-year onset evaluation closes the question — DSTAR-GRU has no defensible superiority on overall MAE, 1h onset (loses to own LSTMEncoder/NoTopology ablations), or 24h onset (significantly beaten by NoSiamese/NoRetrievalBank/LSTM/MLP; retrieval pulls predictions toward persistence and hurts day-ahead warning). Awaiting user choice: (A) framework/tool paper framing, (B) shelve, (C) task change to SDWPF wind forecasting.

## Decision Execution (2026-07-15)

**P1 Route A executed (user decision)**: p1 pivots from a method-superiority paper to a **framework/tool paper** — "An Operating-State Retrieval Framework and Reproducible Curtailment-Risk Benchmark for Power System Decision Support" (per review option 7.3-3, "operating-state retrieval as a service"). Contribution triad: (a) method-agnostic reproducible public curtailment-risk benchmark (full-year RTS-GMLC, fixed 70% SNSP-type reference policy, onset-slice protocol, 10-seed Mann-Whitney/Holm); (b) the Siamese retrieval framework with a significance-backed characterization of its scale-dependent utility (1h significantly beneficial, 24h onset significantly harmful); (c) honest negative findings (persistence/simple baselines dominate overall MAE and day-ahead onset) as proof of benchmark discriminative power. DSTAR-GRU is retained only as the name of the retrieval component. No dispatch-optimization, topology-uncertainty, OPF-feasibility, or overall-forecasting superiority claims. Target IEEE Access (framework precedent), backup Applied Sciences. Only ARA logic/registry files changed (manifest.csv, PAPER.md, claims.md, problem.md, method.md, JOURNAL_REVIEW.md); experiment code and evidence untouched. Remaining before drafting: three-strand related work, SNSP-cap sensitivity sweep (0.6/0.7/0.8), figures, manuscript drafting, experiments.md ablation-label fix.
