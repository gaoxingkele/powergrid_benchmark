# Audit and Pivot an Assumption
_id: `assumption_audit_and_pivot` · journal=`mdpi-algorithms` · primary_count=2_

**Plain alias**. _Audit the load-bearing assumption and pivot_

**Operational signature**. identify implicit assumption → relax/violate → re-derive

**When to apply (in `mdpi-algorithms`)**. When local accepted papers in this venue execute this move (see examples). Prefer composing with a journal-house pattern below.

## Success conditions (from local accepted sample)
- Contribution names a concrete bottleneck and closes it with the operational signature above.
- Evidence matches venue bar (baselines / case / figures) as observed in tagged papers.
- Differentiation stated vs nearest prior stack (not only 'we combine X+Y').

## Failure modes (desk-reject risks adapted from IdeaSpark reject lessons)
- Pattern executed as a mechanical wrapper of known modules without new identifying structure.
- Preservation/claims supported only by a single toy case or confounded ablation.
- True bottleneck left untouched while swapping a surface operator.

## Local examples
- Anomaly Detection in High-Dimensional Time Series Data with (`mdpi-algorithms__2025__anomaly_detection…`) — bottleneck: optimization tractability / constraint satisfaction
- Inferring the Timing of Antiretroviral Therapy by Zero-Inflated (`mdpi-algorithms__2025__inferring_the_tim…`) — bottleneck: forecasting accuracy / uncertainty under nonstationarity
