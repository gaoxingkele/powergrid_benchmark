# Figure 14: Priority indices obtained from each node iteration

**Source:** Section 4.3, p.21 — Figure 14 caption

**Figure type:** quantitative_plot

**Extraction method:** exact_from_labels

**Reading confidence:** high

**Screenshot:** ![Figure 14](figures/figure14.png)

**Description:** Priority indices obtained from each node iteration. (a) Node-level priority index vs. node number. (b) Block-level priority index vs. block number. Shows which nodes selected across sequential iterations.

**Visual description:** Two subplots: (a) scatter/bar of node priority, (b) aggregated block priority. Axes: x=node/block ID, y=priority index value (linear scale).

**Trend summary:** Priority indices obtained from each node iteration. (a) Node-level priority index vs. node number. (b) Block-level priority index vs. block number. Shows which nodes selected across sequential iterations.

**Claims supported:** C01, C02
